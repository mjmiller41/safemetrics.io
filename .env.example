# Clerk publishable key, read by Vite at build time. Publishable, not a secret, but
# instance-specific: it must match the CLERK_ISSUER in wrangler.jsonc or the worker
# will reject every token the browser mints.
VITE_CLERK_PUBLISHABLE_KEY=pk_test_xxx
